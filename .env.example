# Keep demo as the safe default. "live" is reserved for a future provider integration.
NEXT_PUBLIC_DEFAULT_SOURCE_MODE=demo

# Server-only credential placeholder. Never prefix secrets with NEXT_PUBLIC_.
FOOD_DATA_API_KEY=
