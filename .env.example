# Optional server-only Amap Web Service credential. When present, the page
# automatically combines live Amap POIs with clearly labeled demo-only
# Meituan, Ele.me, and Douyin adapters. Never prefix secrets with NEXT_PUBLIC_.
AMAP_WEB_SERVICE_KEY=
